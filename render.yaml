services:
  - type: web
    name: ai-healthmate-voice
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn voice_api.main:app --host 0.0.0.0 --port $PORT
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
